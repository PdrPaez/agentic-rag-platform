# Contributing

Use the next sequential `ARAG-XXX` card for meaningful work. Branch from `dev` with a descriptive prefix, validate the change, commit with `[AREA][ARAG-XXX] description`, and integrate into `dev` before promotion to `main`.

Keep changes focused, update tests and documentation when behavior changes, and never commit secrets, local databases, build output, or credentials.

